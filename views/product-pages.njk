{% extends "layout.njk" %}
{% block  content %}

{%set title = product.name %}

 <header class="d-flex flex-wrap justify-content-center py-3 mb-4 border-bottom">
      <a href="/" class="d-flex align-items-center mb-3 mb-md-0 me-md-auto link-body-emphasis text-decoration-none">
       
        <div class="home">
            <h1> Flip Flops </h1>
            <hr>
            <div>
            <p class="text"> Put Your Flip in the Flop </p>
          </div>
        </div>

      </a>

      <ul class="nav nav-pills">
        <li class="nav-item"><a href="#" class="nav-link active" aria-current="page">Home</a></li>
        <li class="nav-item"><a href="#" class="nav-link"></a></li>
        
      </ul>
    </header>



<div class="grid-container2">

    <div class="separate">
        {% set imageUrl = "./src/img/" + product.name + "/" + product.images[0].url %}
        {% image imageUrl, product.images[0].alt %}

        {% set imageUrl = "./src/img/" + product.name + "/" + product.images[1].url %}
        {% image imageUrl, product.images[1].alt %}

    </div>

    <div>
        <div class="card1">
            <p class="price">{{ product.name }} är {{ product.price }}kr</p>
            <div class="text1">
                <p>Beskrivning:</p>
                <p>{{ product.description }}</p>
            </div>
        </div>

        <a href="{{ product.url }}">
            <div class="card">
                <button>Original Site</button>
            </div>
        </a>

    </div>


</div>

{% endblock %}